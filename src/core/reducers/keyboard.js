import * as commands from 'core/keyboard/constants';
import defaultCommands from 'core/keyboards/defaults';

export default function keyboardConfigReducer(
    state = defaultCommands,
    action,
) {
    return state;
}
